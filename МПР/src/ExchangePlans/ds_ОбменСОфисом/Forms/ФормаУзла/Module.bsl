
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	УправлениеВидимостью();
	
КонецПроцедуры

&НаКлиенте
Процедура ОбщееСообщениеПриИзменении(Элемент)
	
	УправлениеВидимостью();
	
КонецПроцедуры

&НаСервере
Процедура УправлениеВидимостью()
	
	Элементы.Магазины.Видимость = Не Объект.ОбщееСообщение;
	Элементы.Получатели.Видимость = Объект.ОбщееСообщение;
	
КонецПроцедуры	